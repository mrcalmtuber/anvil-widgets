kind = "web"
previewPath = "/"
title = "Widget Hub"
version = "1.0.0"
id = "artifacts/widget-hub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22490

[services.development]
run = "pnpm --filter @workspace/widget-hub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/widget-hub", "run", "build" ]
publicDir = "artifacts/widget-hub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22490"
BASE_PATH = "/"
